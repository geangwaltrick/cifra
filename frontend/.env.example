# Copie para .env.local (desenvolvimento) ou defina na plataforma de deploy.
#
# O Vite embute esta variavel no bundle em tempo de build, nao em execucao:
# trocar a URL exige rebuild do front, nao so reiniciar.
VITE_API_URL=http://localhost:8080
