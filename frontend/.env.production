# Usado automaticamente pelo `vite build`.
#
# Precisa existir aqui, versionado, porque o Vite embute esta variavel no
# arquivo em tempo de build: configurar so no painel da plataforma nao ajuda
# quando o build sai da maquina do desenvolvedor -- e ai o front publicado
# aponta para localhost e nao acha servidor nenhum.
#
# Nao e segredo: e o endereco publico da API.
VITE_API_URL=https://cifra-54lq.onrender.com
